{% extends '::admin.html.twig' %}
{% block title %}Створення Типу Категорій{% endblock %}
{% block body -%}

    <a class="btn btn-primary back-btn" href="{{ path('type') }}">{{ 'Назад' }}</a>
    <br>
    <h3>Створення Типу Категорій</h3>

    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    <div class="admin-form">
        {{ form(form) }}
    </div>
{% endblock %}