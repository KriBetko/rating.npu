{% extends '::admin.html.twig' %}
{% block title %} Групи критеріїв{% endblock %}
{% block body -%}
    <h1>Групи кртеріїв</h1>
    <a class="btn btn-success pull-right" href="{{ path('group_new') }}">{{ 'Додати Групу' }}</a>
    <br><br><br>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Назва</th>
                <th>Дії</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td class="col-md-1"><a href="{{ path('group_edit', { 'id': entity.id }) }}">{{ loop.index }}</a></td>
                <td>{{ entity.title }}</td>
                <td class="col-md-1">
                    <a class="btn btn-info btn-mini" href="{{ path('group_edit', { 'id': entity.id }) }}">
                        <span  class="glyphicon glyphicon-edit" aria-hidden="true"></span>
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
