{% extends '::admin.html.twig' %}
{% block title %}Створення Категорії{% endblock %}
{% block body -%}
    <a class="btn btn-primary back-btn" href="{{ path('category') }}">{{ 'Назад' }}</a>
    <br>
    <h3>Створення Категорії</h3>

    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    <div class="admin-form">
        {{ form(form) }}
    </div>
{% endblock %}