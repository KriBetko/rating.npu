{% extends '::admin.html.twig' %}
{% block title %}Створення Критерія{% endblock %}
{% block body -%}
    <a class="btn btn-primary back-btn" href="{{ path('criterion_create') }}">{{ 'Назад' }}</a>
    <br>
    <h3>Редагування Критерія</h3>

    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    <div class="admin-form">
        {{ form(form) }}
    </div>
{% endblock %}