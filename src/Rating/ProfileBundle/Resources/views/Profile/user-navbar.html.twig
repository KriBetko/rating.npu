<div class="container">
    <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
            {% set currentPath = app.request.attributes.get('_route') %}
            <li {% if currentPath == 'my_profile' %}class="active"{% endif %}>
            <a href="{{ path('my_profile') }}">Мій Профіль</a>
            </li>
            <li {% if currentPath == 'my_measure' %}class="active"{% endif %}>
            <a href="{{ path('my_measure') }}">Особова картка</a>
            </li>

            {% if is_granted('ROLE_ADMIN') %}
            <li {% if currentPath == 'cathedras_list' %}class="active"{% endif %}>
            <a href="{{ path('cathedras_list') }}">Підрозділи</a>
            </li>
            <li role="presentation" class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                    Рейтинги<span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                    <li><a href="{{ path('rating_user') }}">Викладачів</a></li>
                    <li><a href="{{ path('my_profile') }}">Кафедр</a></li>
                    <li><a href="{{ path('my_profile') }}">Факультетів</a></li>
                    <li><a>Студентів</a></li>
                </ul>
            </li>
            {% endif %}
        </ul>
        <ul class="nav navbar-nav navbar-right">
            {% if is_granted('ROLE_ADMIN') %}
            <li {% if currentPath == 'admin_index' %}class="active"{% endif %}>
            <a href="{{ path('admin_index') }}">
                Адміністративна панель
                <span class="glyphicon glyphicon-wrench" aria-hidden="true"></span>
            </a>
            </li>
            {% endif %}
            <li>
                <a href="{{ path('logout') }}">
                    Вийти
                    <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span>
                </a>
            </li>
        </ul>
    </div>
</div>