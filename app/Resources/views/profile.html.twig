<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {% stylesheets
            '@RatingMainBundle/Resources/public/css/reset.css'
            '@bootstrap_css'
            '@bootstrap_theme_css'
            'bundles/ratingmain/css/jquery-ui.min.css'
            '@RatingMainBundle/Resources/public/less/*'
            output='css/general.css'
            %}
            <link href="{{ asset_url }}" type="text/css" rel="stylesheet" />
            {% endstylesheets %}
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div>
            <header class="wrapper container">
                <h3>Рейтенгова система НПУ ім. М. П. Драгоманова</h3>
                {% set currentPath = app.request.attributes.get('_route') %}
                <ul class="nav nav-tabs">
                    <li role="presentation" {% if currentPath ==  'my_profile' %}class="active" {% endif %}><a href="{{ path('my_profile') }}">Мій Профіль</a></li>
                    <li role="presentation" {% if currentPath ==  'my_measure' %}class="active" {% endif %}><a href="{{ path('my_measure') }}">Особова картка</a></li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li role="presentation"   {% if currentPath ==  'cathedras_list' %}class="active" {% endif %}><a href="{{ path('cathedras_list') }}">Підрозділи</a></li>
                        <li role="presentation"   {% if currentPath ==  'rating_user' %}class="active" {% endif %}><a href="{{ path('rating_user') }}">Рейтинг викладачів</a></li>
                        <li role="presentation"   {% if currentPath ==  'rating_student' %}class="active" {% endif %}><a href="{{ path('rating_student') }}">Рейтинг студентів</a></li>
                    {% endif %}

                    <li role="presentation" class="navbar-right" > <a href="{{ path('logout') }}">Вийти</a></li>

                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="navbar-right" role="presentation"><a href="{{ path('admin_index') }}">Адміністративна панель</a></li>
                    {% endif  %}
                </ul>
            </header>
        </div>
        <div style="margin-top: 25px" id="wrapper" class="container">
            {% block body %}{% endblock %}
        </div>
        {% block javascripts %}
            {% javascripts
            '@jquery_js'
            '@jqueryui_js'
            '@bootstrap_js'
            '@RatingMainBundle/Resources/public/scripts/ajax.form.js'
            '@RatingMainBundle/Resources/public/scripts/app.js'
            %}
            <script src="{{ asset_url }}"></script>
            {% endjavascripts %}
        {% endblock %}
    </body>
</html>
