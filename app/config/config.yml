imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

framework:
    #esi:             ~
    #translator:      { fallbacks: ["%locale%"] }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    form:
            resources: ['bootstrap_3_layout.html.twig']



# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    read_from:      "%kernel.root_dir%/../web"
    filters:
        cssrewrite: ~
        lessphp:
            apply_to: "\.less$"
            formatter: "compressed"
            preserve_comments: false
# Doctrine Configuration
doctrine:
    dbal:
        driver:   "%database_driver%"
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: gmail
    username:  npu.rating@gmail.com
    password:  qazxswedc12

fos_user:
    db_driver: orm
    firewall_name: main
    user_class: Rating\UserBundle\Entity\User
    registration:
        form:
            type: rating_user_registration
            validation_groups: [RatingRegistration]
        confirmation:
            from_email:
                address:        admin@npu.edu.ua
                sender_name:    Admin
            enabled: true
            template:   RatingUserBundle:User:confirm.email.html.twig
    resetting:
          email:
            template: RatingUserBundle:Resetting:resetting.email.twig
            from_email:
              address:        admin@npu.edu.ua
              sender_name:    Admin

    profile:
        form:
            type: rating_user_profile
            validation_groups: [RatingProfile]
    service:
            mailer: fos_user.mailer.twig_swift

services:
  fos_user.listener.email_confirmation:
          class:        "Rating\UserBundle\EventListener\EmailConfirmationListener"
          arguments:    ["@fos_user.mailer", "@fos_user.util.token_generator", "@router", "@session", "@session"]
          tags:
              - { name: kernel.event_subscriber }
